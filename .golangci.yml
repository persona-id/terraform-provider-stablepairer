formatters:
  enable:
    - gofmt
  exclusions:
    generated: lax
    paths:
      - builtin$
      - examples$
      - third_party$
issues:
  max-issues-per-linter: 0
  max-same-issues: 0
linters:
  default: none
  enable:
    - copyloopvar
    - durationcheck
    - errcheck
    - forcetypeassert
    - godot
    - govet
    - ineffassign
    - makezero
    - misspell
    - nilerr
    - predeclared
    - staticcheck
    - unconvert
    - unparam
    - unused
  exclusions:
    generated: lax
    paths:
      - builtin$
      - examples$
      - third_party$
    presets:
      - comments
      - common-false-positives
      - legacy
      - std-error-handling
version: "2"
